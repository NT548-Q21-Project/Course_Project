apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: recruitment-service-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secretsmanager
    kind: SecretStore
  target:
    name: recruitment-service-secret
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: dev-recruitment-db
        property: DATABASE_URL
