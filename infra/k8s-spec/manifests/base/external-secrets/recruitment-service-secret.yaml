apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret

metadata:
  name: recruitment-service-secret

spec:
  refreshInterval: 1h

  secretStoreRef:
    name: aws-secretsmanager
    kind: SecretStore

  target:
    name: recruitment-service-secret

  dataFrom:
    - extract:
        key: recruitment-service/prod