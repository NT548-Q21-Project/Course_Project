name: Recruitment Service CI

on:
  push:
    branches:
      - main
      - develop
    paths:
      - "backend/services/recruitment-service/**"
      - "docker-compose.yml"
      - ".github/workflows/recruitment-service-ci.yml"

  pull_request:
    branches:
      - main
      - develop
    paths:
      - "backend/services/recruitment-service/**"
      - "docker-compose.yml"
      - ".github/workflows/recruitment-service-ci.yml"

  workflow_dispatch:

permissions:
  contents: read

env:
  PYTHON_VERSION: "3.12"
  SERVICE_PATH: backend/services/recruitment-service
  IMAGE_NAME: aimatch-recruitment-service
  IMAGE_TAG: ${{ github.sha }}
  TEST_DATABASE_URL: postgresql+psycopg://postgres:postgres@localhost:5432/aimatch_test_db
jobs:
  quality-and-test:
    name: Quality and Test
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash
        working-directory: ${{ env.SERVICE_PATH }}

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: aimatch_test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d aimatch_test_db"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "0.5.31"
          enable-cache: true
          cache-dependency-glob: "${{ env.SERVICE_PATH }}/pyproject.toml"

      - name: Set up Python
        run: uv python install ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: make ci-install

      - name: Run quality checks
        run: make check

      - name: Run tests
        run: make test

  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: quality-and-test

    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build recruitment-service image
        uses: docker/build-push-action@v6
        with:
          context: ./backend/services/recruitment-service
          file: ./backend/services/recruitment-service/Dockerfile
          push: false
          tags: |
            ${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}
          cache-from: type=gha,scope=recruitment-service
          cache-to: type=gha,mode=max,scope=recruitment-service